@model IEnumerable<ReceptBank.Models.ReceptModel>

<h2>Recept List</h2>

<table class="table">
    <thead>
        <tr>
            <th>Recept</th>
            <th>Ingredients</th>
         
        </tr>
    </thead>
    <tbody>
        @foreach (var Recept in Model)
        {
            <tr>
                <td>@Recept.Name</td>
                <td>@Recept.Ingredients</td>                
            </tr>
        }
    </tbody>
</table>
