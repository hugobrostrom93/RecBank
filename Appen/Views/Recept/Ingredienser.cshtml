@model ReceptBank.Models.ReceptViewModel

<style>
    /* Styling for the page */
    body {
        font-family: 'Comic Sans MS', cursive;
        background-color: #ffb6c1; /* Baby pink background color */
        color: #0099cc; /* Baby blue text color */
    }

    /* Style for the table header */
    .table thead {
        background-color: #ffb6c1; /* Baby pink background color */
        color: #fff; /* White text color */
    }

    /* Style for table rows */
    .table tbody tr {
        background-color: #e6f7ff; /* Lighter baby blue background color */
    }

    /* Style for table cell (td) */
    .table tbody td {
        color: #ff69b4; /* Pink text color */
    }

    /* Text shadow for better readability */
    h2, .table th, .table td {
        text-shadow: 1px 1px 2px rgba(0, 0, 0, 0.5); /* Subtle text shadow */
    }
</style>

<h2>Recept List</h2>

<table class="table">
    <thead>
        <tr>
            <th>Recept</th>
            <th>Ingredients</th>
        </tr>
    </thead>
    <tbody>
        <tr>
            <td>@Model.Name</td>
            <td>@Model.Ingredients</td>
        </tr>
    </tbody>
</table>
